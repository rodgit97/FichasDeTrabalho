extends layout //-- extends a layout template named "layout". This means that the content defined in this template will be inserted into the layout template at the appropriate location.

block content
  h1= message
  h2= error.status
  pre #{error.stack}
